import http from "node:http";import {PORT} from "./config.js";import {attach} from "./websocket/websocketServer.js";const server=http.createServer((req,res)=>{res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Headers','*');if(req.url==='/health'){res.writeHead(200,{'Content-Type':'application/json'});res.end(JSON.stringify({status:'ok',game:'LUMI'}));return}res.writeHead(404);res.end('LUMI SERVER')});attach(server);server.listen(PORT,()=>console.log(`LUMI server listening on ${PORT}`));