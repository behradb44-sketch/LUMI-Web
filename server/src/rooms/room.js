import {OBJECTIVES} from "../game/objectives.js";import {EVENTS} from "../game/gameEvents.js";const code=()=>Math.random().toString(36).slice(2,8).toUpperCase();export class Room{constructor(c=code()){this.code=c;this.players=new Map();this.objective=structuredClone(OBJECTIVES[Math.floor(Math.random()*OBJECTIVES.length)]);this.event=EVENTS[Math.floor(Math.random()*EVENTS.length)];this.buildings=[];this.nextEvent=Date.now()+30000+Math.random()*45000}add(p){this.players.set(p.id,p)}remove(id){this.players.delete(id)}broadcast(o,except){const s=JSON.stringify(o);for(const p of this.players.values())if(p.id!==except&&p.ws?.readyState===1)p.ws.send(s)}snapshot(){return [...this.players.values()].map(p=>({id:p.id,name:p.name,character:p.character,color:p.color,x:p.x,y:p.y,state:p.state,mic:p.mic}))}tick(){if(Date.now()>=this.nextEvent){this.event=EVENTS[Math.floor(Math.random()*EVENTS.length)];this.nextEvent=Date.now()+45000+Math.random()*60000;this.broadcast({type:'event',event:this.event})}}}